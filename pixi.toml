[workspace]
authors = ["Sierra Arn"]
channels = ["conda-forge"]
name = "redis-core"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
python = "*"
pydantic-settings = "*"
redis-py = "*"
just = "*"

[feature.test.dependencies]
ipykernel = "*"

[environments]
test = ["test"]